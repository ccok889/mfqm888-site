# 模仿笔迹网站项目交接与恢复说明

最后更新：2026-08-31

这份文件用于更换电脑、重新安装 Codex 或对话记录不可用时恢复项目。继续工作前，应先阅读本文件和仓库中的 `DEPLOYMENT.md`。

## 1. 项目概况

- 网站名称：模仿笔迹网
- 正式网址：<https://www.mfqm888.com>
- 网站形式：纯静态 HTML、CSS、JavaScript
- GitHub 账户：`ccok889`
- GitHub 仓库：`ccok889/mfqm888-site`
- 仓库地址：<https://github.com/ccok889/mfqm888-site>
- 生产分支：`main`
- 部署方式：GitHub + Cloudflare Pages
- Cloudflare Pages 项目：`mfqm888-site`
- Pages 临时域名：<https://mfqm888-site.pages.dev>

本文件不保存密码、访问令牌、验证码、恢复代码或私钥。

## 2. 业务定位与固定关键词

网站内容围绕以下四个核心关键词展开，不能擅自替换、弱化或改成其他业务方向：

1. 模仿笔迹
2. 模仿签名
3. 模仿签字
4. 笔迹鉴定

栏目名称以“模仿签名”为主，不要擅自改成“签名设计”。文章可以根据实际主题扩展自然长尾词，但标题、正文和内链仍需围绕上述四个核心关键词。

## 3. 网站内容结构

主要目录：

```text
index.html                  首页
category/                   业务栏目和子栏目
article/                    文章详情页
case/                       案例展示
news/                       资讯中心
city/                       城市服务与分站内容
contact/                    联系我们
about/                      关于我们
assets/css/                 全站样式
assets/js/main.js           导航、搜索及前端文章索引
assets/images/              网站图片
search.json                 站内搜索数据
sitemap.xml                 搜索引擎站点地图
robots.txt                  抓取规则
DEPLOYMENT.md               部署与域名资料
SEO-EXTERNAL-LINKS.md       外部查询记录
网站签名.md                 本交接文件
```

## 4. 内容编写规则

- 使用自然、具体的人类语言，不写空泛套话，不使用明显的模板化 AI 句式。
- 标题必须对应实际内容，不能只堆叠关键词。
- 正文围绕真实问题展开，合理使用 H2、H3、FAQ 和相关文章内链。
- 模仿笔迹、模仿签名、模仿签字和笔迹鉴定应按文章主题自然出现，不能为了密度机械重复。
- 案例展示与资讯中心不能重复调用同一批内容：案例重在材料、过程和结果说明；资讯重在更新、方法、资料准备和专题内容。
- 城市页面要结合城市名称与具体服务内容编写，不能出现其他行业内容，也不能简单复制主站或其他城市页面。
- 新文章发布后要同步更新对应栏目、首页或资讯列表、`search.json`、`assets/js/main.js` 和 `sitemap.xml`。
- 不在页面中加入“备案号请上线前替换”等占位文字。

## 5. 图片规则

- 每篇文章必须使用独立主图，不得与其他文章重复引用同一图片。
- 新文章发布前，应检查全站文章主图引用和图片文件哈希。
- 不允许仅修改文件名来冒充新图片。
- 新图片应与文章主题直接相关，可以围绕手写签字、模仿签名、手写文件、手写笔迹等内容生成。
- 图片中避免真实姓名、品牌标志、水印和无关文字。
- 图片应压缩为适合网页加载的 JPG 或 WebP，避免直接上传体积过大的 PNG 原图。
- 文章正文主图、列表缩略图、`og:image` 和 Article JSON-LD 的 `image` 必须保持一致。
- 图片必须设置与文章主题相符的 `alt` 文本。

截至 2026-08-31：

- 已检查文章：48 篇
- 文章主图重复组：0
- 最近一次全站图片修正提交：`9d57706`
- 最近新增的 25 张历史文章配图已从约 62MB 优化至约 8.3MB

## 6. SEO 固定要求

- 所有正式 canonical 使用 `https://www.mfqm888.com`。
- 裸域名 `mfqm888.com` 通过 Cloudflare 301 跳转到 `www.mfqm888.com`，并保留路径与查询参数。
- 页面应包含独立的 title、description、canonical 和必要的 Open Graph 标签。
- 文章页应包含 Article、BreadcrumbList；存在真实 FAQ 内容时可以包含 FAQPage。
- 栏目、文章、案例、资讯和城市页面之间保持清晰内链。
- 图片、CSS、JavaScript 和站内链接不得出现 404。
- 修改网址结构时同步更新 canonical、站点地图、站内链接和旧网址 301 规则。

## 7. 联系方式

- 微信图片显示内容：`pep950`
- 电话图片显示内容：`13725706247`
- 联系方式应以图片形式展示，相关图片位于 `assets/images/`。

## 8. 部署规则

Cloudflare Pages 当前配置：

- GitHub 仓库：`ccok889/mfqm888-site`
- 生产分支：`main`
- 框架预设：无
- 构建命令：留空
- 构建输出目录：`/`
- 推送 `main` 后自动部署

HTTPS 由 Cloudflare 自动签发和续期。域名、301 跳转、HTTPS 检查位置及城市二级域名规划详见 `DEPLOYMENT.md`。

## 9. 日常发布流程

1. 检查 Git 工作区，避免覆盖无关改动。
2. 撰写与栏目定位匹配的新文章。
3. 为每篇文章制作独立图片并进行网页压缩。
4. 更新文章页、列表页、搜索索引、结构化数据和站点地图。
5. 检查文章标题、描述、关键词、内链和图片三次。
6. 审计重复图片、无效链接、缺失文件和 HTML 基础结构。
7. 执行 `git diff --check`。
8. 提交到 Git，并推送到 `main`。
9. 等待 Cloudflare Pages 自动部署。
10. 打开正式网址检查首页、修改页面、图片和移动端显示。

## 10. 新电脑恢复步骤

### 安装与克隆

在新电脑安装 Git 和 Codex，使用当前 GitHub 账号与当前 ChatGPT/OpenAI 账号登录，然后执行：

```bash
git clone https://github.com/ccok889/mfqm888-site.git
cd mfqm888-site
git status
git log -5 --oneline
```

正常情况下，`git status` 应显示本地分支与 `origin/main` 一致。

### 在 Codex 中继续

1. 在 Codex 中添加或打开刚克隆的 `mfqm888-site` 文件夹。
2. 让 Codex 先完整阅读 `网站签名.md` 和 `DEPLOYMENT.md`。
3. 再检查 `git status -sb`、最新提交和正式网站状态。
4. 如果原对话仍在同一账号的任务列表中，可以打开原对话继续；即使原对话不可用，也以本文件和 Git 历史为准恢复工作。

推荐给新电脑 Codex 的首条指令：

> 请先完整阅读网站签名.md和DEPLOYMENT.md，检查 Git 状态及网站目录，不要修改四个核心关键词，也不要让不同文章重复使用图片。确认当前状态后继续维护 www.mfqm888.com。

## 11. 更换电脑前的检查

旧电脑停止使用前执行：

```bash
git status -sb
git log -1 --oneline
git push origin main
```

必须确认：

- 没有尚未提交的重要文件。
- 当前分支没有显示 `ahead`。
- GitHub 仓库能看到最新提交。
- Cloudflare Pages 最新生产部署成功。
- 正式网站可以正常打开。

如有本地生成但尚未采用的草稿图片、截图或临时文件，应另行复制到移动硬盘或云盘；未加入 Git 的文件不会自动出现在新电脑中。

## 12. 发生冲突时的优先级

如果以后对话记忆、页面旧内容与本文件存在差异，按以下顺序判断：

1. 用户当前明确要求
2. GitHub `main` 分支中的最新代码
3. 本文件与 `DEPLOYMENT.md`
4. 旧对话记录

任何涉及域名切换、删除文件、批量改网址、改变核心关键词或重构城市站的操作，都应先确认现有线上状态和用户当前要求。
